package com.snowrepo.gateway;

import org.springframework.context.annotation.Configuration;

// CORS is handled by WebConfig (CorsWebFilter) and OAuth2Config (Spring Security cors()).
@Configuration
public class SecurityConfig {
}
